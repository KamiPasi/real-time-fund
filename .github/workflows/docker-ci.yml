name: Docker CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  dockerfile-check:
    name: Build & Test Dockerfile
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Create .env
        run: |
          cat << 'EOF' > .env
          NEXT_PUBLIC_LOGIN_ACCOUNT=${{ secrets.NEXT_PUBLIC_LOGIN_ACCOUNT }}
          NEXT_PUBLIC_LOGIN_PASSWORD=${{ secrets.NEXT_PUBLIC_LOGIN_PASSWORD }}
          NEXT_PUBLIC_ENABLE_SERVER_FILE_STORAGE=true
          CONFIG_STORAGE_FILE=/app/data/config-store.json
          NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=${{ secrets.NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY }}
          NEXT_PUBLIC_GA_ID=${{ secrets.NEXT_PUBLIC_GA_ID }}
          NEXT_PUBLIC_GITHUB_LATEST_RELEASE_URL=${{ secrets.NEXT_PUBLIC_GITHUB_LATEST_RELEASE_URL }}
          EOF

      - name: Build Dockerfile image
        run: docker build -t real-time-fund .

      - name: Run Dockerfile container
        run: |
          docker run -d --name fund -p 3000:3000 --env-file .env real-time-fund
          echo "Waiting 10 seconds for the container to start..."
          sleep 10

      - name: Test service
        run: |
          curl -f http://localhost:3000 || (docker logs fund && exit 1)

      - name: Cleanup
        run: |
          docker stop fund
          docker rm fund

  docker-compose-check:
    name: Build & Test Docker Compose
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Create .env
        run: |
          cat << 'EOF' > .env
          NEXT_PUBLIC_LOGIN_ACCOUNT=${{ secrets.NEXT_PUBLIC_LOGIN_ACCOUNT }}
          NEXT_PUBLIC_LOGIN_PASSWORD=${{ secrets.NEXT_PUBLIC_LOGIN_PASSWORD }}
          NEXT_PUBLIC_ENABLE_SERVER_FILE_STORAGE=true
          CONFIG_STORAGE_FILE=/app/data/config-store.json
          NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=${{ secrets.NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY }}
          NEXT_PUBLIC_GA_ID=${{ secrets.NEXT_PUBLIC_GA_ID }}
          NEXT_PUBLIC_GITHUB_LATEST_RELEASE_URL=${{ secrets.NEXT_PUBLIC_GITHUB_LATEST_RELEASE_URL }}
          EOF

      - name: Docker Compose up
        run: |
          docker compose up -d --build
          echo "Waiting 10 seconds for services to start..."
          sleep 10

      - name: Test app service
        run: |
          curl -f http://localhost:3000 || (docker compose logs app && exit 1)

      - name: Cleanup
        run: docker compose down
